import { BlockDurationDto } from './BlockDurationDto';
export declare class MosaicPropertiesBuilder {
    flags: number;
    divisibility: number;
    duration: BlockDurationDto;
    constructor(flags: number, divisibility: number, duration: BlockDurationDto);
    static loadFromBinary(payload: Uint8Array): MosaicPropertiesBuilder;
    getFlags(): number;
    getDivisibility(): number;
    getDuration(): BlockDurationDto;
    getSize(): number;
    serialize(): Uint8Array;
}
